# We need to process a list of Event objects using their attributes
# to generate a report that list all users currently logged in to the machines

numbers = [4, 6, 2, 7, 7]
numbers.sort()
print(numbers)

names = ["Carlos", "Ray", "Alex", "Kelly"]
print(sorted(names))
print(names)
print(sorted(names, key=len))
