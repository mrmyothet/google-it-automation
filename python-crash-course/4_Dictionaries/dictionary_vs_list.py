# If you've got a list of information you'd like to collect and use in your script
# then a list is probably the right approach

ip_addresses = ["192.168.1.1", "127.0.0.1", "8.8.8.8"]

host_addresses = {
    "router": "192.168.1.1",
    "localhost": "127.0.0.1",
    "google": "8.8.8.8",
}

# Use dictionaries when you plan on searching for a specific element.

# Set
# Used when you want to store a bunch of elements and
# be certain that they're only present once

# A sequential, mutable collection of any data type => List
# A sequential, immutable collection of textual data => String
# An unordered collection of unique elements => Set
# A sequential, immutable collection of any data type => Tuple
# Stores key:value pairs =>
